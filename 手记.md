# StarryOS riscv64 QEMU 内核 GDB 调试

## 推荐启动方式

`qemu-riscv64` 是构建配置（不是 QEMU 运行配置）。它已经设置
`DWARF=y` 和 `BACKTRACE=y`，因此会传入 `-Cdebuginfo=2`、
`-Cstrip=none` 以及 frame pointer；`--debug` 另外选择 Cargo 的 `debug`
profile，适合单步和查看局部变量。

```bash
cargo xtask starry qemu \
  --config qemu-riscv64 \
  --debug \
  --rootfs tmp/axbuild/rootfs/rootfs-riscv64-alpine.img/rootfs-riscv64-alpine.img
```

本仓库默认的运行配置
`os/StarryOS/configs/qemu/qemu-riscv64.toml` 已带 `-s`，即 QEMU 会在
`tcp:127.0.0.1:1234` 提供 GDB remote stub。上面的命令构建完成后，内核
ELF 是：

```text
target/riscv64gc-unknown-none-elf/debug/starryos
```

确认调试信息没有被剥离：

```bash
readelf -SW target/riscv64gc-unknown-none-elf/debug/starryos \
  | rg '\.debug_(info|line)|\.symtab'
```

## 在复位点暂停（建议）

仅有 `-s` 会让 QEMU 立即开始运行，可能错过早期启动代码。`starry qemu`
没有 `--qemu-arg` 选项，故复制默认运行配置并只补充 `-S`；该临时文件不修改
仓库配置。

```bash
mkdir -p tmp
sed '/^  "-s",$/a\  "-S",' \
  os/StarryOS/configs/qemu/qemu-riscv64.toml \
  > tmp/qemu-riscv64-gdb.toml

cargo xtask starry qemu \
  --config qemu-riscv64 \
  --debug \
  --qemu-config tmp/qemu-riscv64-gdb.toml \
  --rootfs tmp/axbuild/rootfs/rootfs-riscv64-alpine.img/rootfs-riscv64-alpine.img
```

`-S` 令 CPU 在复位时停止，待 GDB 的 `continue` 后才开始执行。QEMU 运行期间
保留这个终端；另开一个终端连接 GDB。

## 连接 GDB

```bash
riscv64-unknown-elf-gdb \
  target/riscv64gc-unknown-none-elf/debug/starryos
```

```gdb
set pagination off
set architecture riscv:rv64
target remote :1234

# QEMU virt 上内核镜像先位于物理地址 0x80200000。
hbreak *0x80200000
continue

# 建页表并切换到高半虚拟地址后，在带源码信息的 Rust 入口断下。
break someboot::arch::entry::mmu_entry_rust
continue
bt
info registers
list
```

该 RISC-V Starry 内核的早期启动先从物理地址 `0x80200000` 执行，建页表后才
切换到高半虚拟地址；因此第一处使用物理入口，后续源码断点使用 ELF 符号。
`kernel_entry` 是 naked 函数，通常没有可供 `list` 显示的行号，故使用
`mmu_entry_rust` 作为第一个源码级断点。若只调试已经运行的内核，可省略临时
`-S` 配置，直接使用推荐启动命令并执行相同的 `target remote :1234`。

退出 QEMU 串口使用 `Ctrl+A`，再按 `x`。

## SpacemiT K3：同步 StarryOS 到 GRUB2

K3 开发板通过 UEFI GRUB2 启动 StarryOS，不需要 U-Boot，也不要修改 EFI
`BootOrder`。Ubuntu 保持默认启动项；StarryOS 菜单 ID 固定为
`starryos-k3-com260-ifx`。

### 打包

在仓库根目录执行：

```bash
cargo xtask starry grub package --config spacemitk3-com260kit
```

生成目录为 `target/starry-grub/spacemitk3-com260kit/`，其中包含：

```text
starryos.efi
spacemit-k3-com260-ifx.dtb
grub.d/42_starryos
default-grub.d/60-starryos.cfg
```

该配置启用 K3 所需的 `zicbom`、`svpbmt` 和 `efi`，不包含 K3 GMAC。

如果只需指定板子 IP，可直接执行一键同步脚本；SSH 与 `sudo` 密码会在终端中
交互输入，不会写入脚本：

```bash
board_script/k3com260kit/sync_kernel.py 10.42.0.100
```

它会执行上述打包，并在上传后核对分区、DTB 启动参数、文件哈希和 EFI
`BootOrder`；通过预检后才备份、原子安装并运行 `update-grub`。先预览流程：

```bash
board_script/k3com260kit/sync_kernel.py --dry-run 10.42.0.100
```

### 上传和安装

开发板地址为 `ubuntu@10.42.0.100`。先上传打包产物：

```bash
scp target/starry-grub/spacemitk3-com260kit/starryos.efi \
    target/starry-grub/spacemitk3-com260kit/spacemit-k3-com260-ifx.dtb \
    ubuntu@10.42.0.100:/tmp/

scp target/starry-grub/spacemitk3-com260kit/grub.d/42_starryos \
    target/starry-grub/spacemitk3-com260kit/default-grub.d/60-starryos.cfg \
    ubuntu@10.42.0.100:/tmp/
```

登录开发板后，先备份当前包，再安装新文件并重建 GRUB 配置：

```bash
sudo cp -a /boot/starryos /boot/starryos.backup-$(date +%Y%m%d-%H%M%S)

sudo install -m 0755 /tmp/starryos.efi /boot/starryos/starryos.efi
sudo install -m 0644 /tmp/spacemit-k3-com260-ifx.dtb \
  /boot/starryos/spacemit-k3-com260-ifx.dtb
sudo install -m 0755 /tmp/42_starryos /etc/grub.d/42_starryos
sudo install -m 0644 /tmp/60-starryos.cfg /etc/default/grub.d/60-starryos.cfg

sudo update-grub
sudo grub-script-check /boot/grub/grub.cfg
sudo sync
```

安装前应核对 `/dev/sda1` 的 PARTUUID 和 `/dev/sda3` 的 `writable` 标签；若
ESP 的 PARTUUID 改变，需要先更新
`os/StarryOS/configs/board/spacemit-k3-com260-ifx.dts` 并重新打包。

### 一次性测试和回退

仅下一次启动 StarryOS，之后自动回 Ubuntu：

```bash
sudo grub-reboot starryos-k3-com260-ifx
sudo reboot
```

GRUB 菜单中会通过 `search --label writable` 查找根分区，并加载
`/boot/starryos/starryos.efi` 和
`/boot/starryos/spacemit-k3-com260-ifx.dtb`。不使用 initrd。

如果测试失败，物理复位后一次性菜单项已经消费，机器会回到 Ubuntu。需要撤销
部署时，将对应 `/boot/starryos.backup-<timestamp>` 中的文件恢复到
`/boot/starryos`，然后重新执行 `sudo update-grub`。
